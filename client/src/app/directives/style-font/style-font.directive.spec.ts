import { StyleFontDirective } from './style-font.directive';

describe('StyleFontDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleFontDirective();
    expect(directive).toBeTruthy();
  });
});
